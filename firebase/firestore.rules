rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد تطوير مؤقتة لمنصة آفاق.
    // تعمل فقط للمستخدمين الموقّعين Anonymous.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
