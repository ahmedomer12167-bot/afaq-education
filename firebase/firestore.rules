rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد مؤقتة للتطوير فقط.
    // بعد إكمال الربط النهائي يجب تضييق الصلاحيات حسب الدور.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
