const P='afaq31_';function id(){return Date.now().toString(36)+Math.random().toString(36).slice(2,7)}function getData(k){return JSON.parse(localStorage.getItem(P+k)||'[]')}function setData(k,v){localStorage.setItem(P+k,JSON.stringify(v))}function today(){return new Date().toISOString().slice(0,10)}function seedBase(){if(localStorage.getItem(P+'seeded'))return;setData('stages',[{id:id(),name:'الأول متوسط',order:1,status:'مفعلة',visibility:'ظاهر'},{id:id(),name:'الثاني متوسط',order:2,status:'مفعلة',visibility:'ظاهر'},{id:id(),name:'الثالث متوسط',order:3,status:'مفعلة',visibility:'ظاهر'},{id:id(),name:'الرابع الإعدادي',order:4,status:'مفعلة',visibility:'ظاهر'},{id:id(),name:'الخامس الإعدادي',order:5,status:'مفعلة',visibility:'ظاهر'},{id:id(),name:'السادس الإعدادي',order:6,status:'مفعلة',visibility:'ظاهر'}]);setData('subjects',[{id:id(),name:'الأحياء',code:'BIO-12347',stage:'الثالث متوسط',teacher:'غير محدد',color:'أخضر',icon:'🧬',order:1,status:'مفعلة'}]);setData('teachers',[]);setData('students',[]);localStorage.setItem(P+'seeded','1')}function sortByOrder(a,b){return Number(a.order||0)-Number(b.order||0)}function stageUsage(stageName){let students=getData('students').filter(x=>(x.stage||x['المرحلة'])===stageName).length;let subjects=getData('subjects').filter(x=>(x.stage||x['المرحلة'])===stageName).length;let teachers=getData('teachers').filter(x=>(x.stage||x['المرحلة'])===stageName).length;return {students,subjects,teachers,total:students+subjects+teachers}}